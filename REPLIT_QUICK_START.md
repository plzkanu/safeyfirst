# Replit 빠른 배포 가이드

## 🚀 5분 안에 배포하기

### 1단계: Replit에 프로젝트 업로드

**방법 A: 직접 업로드 (가장 간단)**
1. Replit에서 "Create Repl" 클릭
2. "Node.js" 템플릿 선택
3. 파일들을 드래그 앤 드롭으로 업로드
   - `.replit` 파일
   - `replit.nix` 파일
   - `package.json` 파일
   - `server/` 폴더 전체
   - `client/` 폴더 전체
   - 기타 설정 파일들

**방법 B: GitHub 연동 (Git 설치된 경우)**
```bash
# 로컬에서 GitHub에 푸시
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-github-repo-url>
git push -u origin main
```

그 다음 Replit에서:
1. "Create Repl" 클릭
2. "Import from GitHub" 선택
3. 저장소 URL 입력

**방법 C: ZIP 파일로 업로드**
1. 프로젝트 폴더를 ZIP으로 압축 (node_modules 제외)
2. Replit에서 ZIP 파일 업로드
3. Replit이 자동으로 압축 해제

---

### 2단계: 환경 변수 설정

1. Replit 왼쪽 사이드바에서 **"Secrets"** 클릭
2. 다음 변수 추가:

```
JWT_SECRET=your-super-secret-key-here-min-32-chars
NODE_ENV=production
```

**중요**: `JWT_SECRET`은 강력한 랜덤 문자열로 변경하세요!

---

### 3단계: 의존성 설치 및 빌드

Replit 터미널에서 실행:

```bash
# 의존성 설치
npm run install-all

# 프론트엔드 빌드
npm run build
```

---

### 4단계: 실행

Replit에서 **"Run"** 버튼 클릭

또는 터미널에서:
```bash
npm run start:prod
```

---

### 5단계: 확인

1. **Webview** 탭 클릭
2. 로그인 페이지가 표시되는지 확인
3. 기본 계정으로 로그인 테스트:
   - 관리자: `admin` / `admin123`
   - 사용자: `user` / `user123`

---

## ✅ 완료!

이제 Replit의 공개 URL을 통해 어디서나 접속할 수 있습니다!

---

## 🔄 업데이트 배포

로컬에서 개발 후 업데이트:

```bash
# 1. 변경사항 커밋
git add .
git commit -m "업데이트"
git push

# 2. Replit에서 Pull
git pull

# 3. 재빌드 및 재시작
npm run build
# Run 버튼 클릭
```

---

## 🐛 문제 해결

**포트 오류?**
- Replit이 자동으로 포트를 할당합니다. 코드는 이미 `0.0.0.0`으로 설정되어 있습니다.

**빌드 오류?**
```bash
cd client
rm -rf node_modules build
npm install
npm run build
```

**환경 변수 오류?**
- Secrets 탭에서 `JWT_SECRET`과 `NODE_ENV`가 설정되었는지 확인

---

자세한 내용은 `DEPLOY_REPLIT.md`를 참고하세요!

